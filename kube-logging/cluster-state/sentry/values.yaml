super-sentry:
  sentry:
    image:
      tag: "9.0"
    worker:
      replicacount: 1
    service:
      type: ClusterIP
    filestore:
      filesystem:
        persistence:
          enabled: false
          storageClass: "sentry-storage"
    postgresql:
      imageTag: "9.5"
      persistence:
        enabled: false
        storageClass: "sentry-storage"
    redis:
      master:
        persistence:
          enabled: false
          storageClass: "sentry-storage"
    ingress:
      enabled: true
      hostname: "sentry.{{ base_domain }}"